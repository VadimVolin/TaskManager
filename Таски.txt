Привет.

Проверила работу. Есть замечания, описанные ниже.

Исправлять замечания необходимо следуя нескольким простым правилам:

1 замечание = 1 коммит. В комментарии к коммиту следует указать, что именно исправляется и номер замечания.

Также, ожидаю диаграммы со следующими исправлениями. Окончательной версии диаграм не было, они не засчитаны.

Необходимо исправить:

[ПРИЛОЖЕНИЕ]

👌️1. При возникновении ошибки, не нужно их выводить в консоль пользователя. Их нужно фиксировать в логах, но не в диалоге с пользователем.

2. Редактирование.

👌️2.1. Добавить возможность преобразования повтор.задачи в неповтор. и наоборот.

👌️2.2. Разделить редактирование на подпункты. Дата не должна редактироваться внутри меню редактирования названия и т.п.

👌️3. Добавить пробелы в выводе даты

start time: 2020-02-11T01:22:12

👌️4. Как понять задача активная или нет? Добавить информацию об этом.

👌️5. При выводе информации о задаче, следует указывать в каких единицах измеряется интервал повторения.

6. Добавила задачу, не увидела ее в нотификациях и при выводе календаря.

👌️7. Убрать секунды при задании даты, а также разрешить вводить прошедшие даты.

 

[КОД]

 

1. В интерфейсах также следует отображать поля, а не только методы. Пересмотри интерфейсы, во многих из них по одному методу. Их следует объеденить. Также удали пустые интерфейсы, в чем их смысл?

2. Удалить неиспользующийся код. Например, переменные. Зачем их создавать, если они не используются. Пример:



3.  Добавить пункт, который обсуждали при написании диаграмм:

    Т.к. приложение должно сохранять изменения пользователей, предлагаю реализовать следующий вариант выбора действией пользователя при запуске приложения:

    1. продолжить работу над текущим списком задач, который сохранился при предыдущем запуске приложения;

    2. создать новый (пустой) список задач;

    3. загрузить файл со списком задач.

4. Зачем писать новые методы сохранения задач, ведь есть модель и класс «TaskIO». Класс «ReadInputUtil» необходимо или перенести в соответствующий пакет или удалить и распределить функционал по другим классам. Считыванием данных пользователя, проверки их корректности и ответов должно заниматься представление.

5. Обрабатывать ошибки в блоках «if-else» нельзя. Почитай лекцию + памятку к работе.

6. Ресурсы:       

                6.1. Не нужно каждый раз создавать новый «scanner». Его следует обьявить один раз и использовать.

                6.2. Добавить закрытие ресурсов. Читаем лекции + памятку.

7. Зачем в контроллере нотификаций создавать тред, а затем в главном контроллере снова создавать еще один? Тред должен создаваться всего 1 раз.

8. Разделить контроллер – представление. Перенести функционал считывания введенных данных пользователем в представление. Представление получает данные от пользователя, передает их в контроллер, который их обрабатывает. Также, представление не может получать готовый список задач или саму задачу. Представление передает информацию пользователю и получает ее. Моделью пользуется исключительно контроллер.

9. В главном контроллере ты получаешь список со всеми задачи, а затем во вспомогательных контроллер снова его считываешь. Зачем? Считывание должно проиисходить в одном месте, в главном контроллере.

10. Слишком длинные методы. Например, метод редактирования задач. Он включает в себя очень много логик(даты, затем интервала и т.п.). Метод нужно разбить на несколько более мелких.

 

[MAVEN]:

1. Проект должен собираться командой "mvn clean install";

2.  Убрать лишние плагины, которые не используются.

3. Убедится, что собирается jar-файл с зависимостями и коректно запускается. Это должен быть jar-with-dependencies.

 

Архив не запускается, ошибка:

no main manifest attribute, in TaskManager-1.0-SNAPSHOT.jar

 

После изменения архитектуры приложения, более детально проверим код.
